
<interactive>:2:5: error:
    Top-level bindings for unlifted types aren't allowed: a = (# 1 #)

<interactive>:3:5: error:
    Top-level bindings for unlifted types aren't allowed:
      a = (# 1, 3 #)

<interactive>:5:5: error:
    Top-level bindings for unlifted types aren't allowed:
      a = (# 1, 3 #) :: (# Integer, Integer #)
